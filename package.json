{
  "name": "resiliencia-costera-v2",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "api:dev": "npm run start:dev --workspace=apps/api",
    "web:dev": "npm run start --workspace=apps/web"
  }
}
